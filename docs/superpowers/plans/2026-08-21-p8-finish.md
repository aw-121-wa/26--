# 珠峰 P8 正式比赛终点 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with verification checkpoints.

**Goal:** 将第一轮正式比赛路线和珠峰障碍成功路径的终点固定为 P8，播报完成后永久停车。

**Architecture:** 保留现有主干路线宏和珠峰上坡、撞板、手势、后退、语音实现，只在第一轮数组去掉 P8 后返程拼接，并在 `Barrier_HighMountain()` 的语音成功路径增加专用等待与永久停车 helper。失败路径和其他障碍继续使用现有状态恢复逻辑。

**Tech Stack:** STM32 C firmware, FreeRTOS delay API, CMake/Ninja Release build, Python `unittest` source-contract tests.

## Global Constraints

- 第一轮正式路线终点必须是 P8 后的 `ROUTE_END`。
- 保留 `ROUTE_P8_RETURN_UPPER`、`BACK_D2`、`BACK_D3`、`BACK_D4`、`BACK_D5` 和所有 `round2_xxx` 定义。
- `barrier_platform_start_gesture()` 的动作组、顺序、等待时间和调用位置保持不变。
- 珠峰后退仍为 `barrier_reverse_distance(6.0f, 12.0f, heading)`。
- P8 语音映射保持 `VOICE_INDEX_PLATFORM_P8`，不修改语音映射定义。
- 语音后等待 `HIGH_MOUNTAIN_END_VOICE_WAIT_MS`，值为 `3000u`。
- 珠峰终点成功路径不得再调头、下坡、重复摄像头回中或调用 `barrier_complete()`。
- 不修改南极、通用平台、P2、桥、楼梯、门、PID、循迹参数及既有专项修复。
- 不提交、不烧录、不删除旧返程代码。

---

### Task 1: Add failing P8 finish contract tests

**Files:**
- Create: `tests/test_p8_finish_contract.py`
- Modify: `tests/test_polar_mountain_contract.py:25-40`

**Interfaces:**
- Consumes: source text from `App/map/route_catalog.c` and `App/barrier/barrier.c`.
- Produces: executable `unittest` contracts for the route arrays and `Barrier_HighMountain()` success suffix.

- [ ] **Step 1: Write the failing test**

Add a `function_body()` helper that extracts one balanced C function body. Add tests equivalent to:

```python
class P8FinishContractTest(unittest.TestCase):
    def test_first_round_routes_stop_after_p8_mainline(self):
        for name, macro in (
            ("round1_highscore", "ROUTE_TO_HIGH_SCORE"),
            ("round1_highscore_d2", "ROUTE_HIGH_FROM_N13"),
        ):
            match = re.search(
                rf"static const uint8_t {name}\[\] = \{{(.*?)\n\}};",
                self.route,
                re.DOTALL,
            )
            self.assertIsNotNone(match)
            body = match.group(1)
            self.assertEqual(body.count("ROUTE_END"), 1)
            self.assertIn(f"{macro},\n    ROUTE_END", body)
            self.assertNotIn("ROUTE_P8_RETURN_UPPER", body)
            self.assertNotIn("D5, N3, N4, B3, N2, P2", body)

    def test_high_mountain_finishes_after_voice_wait_and_locks_stop(self):
        self.assertIn("#define HIGH_MOUNTAIN_END_VOICE_WAIT_MS 3000u", self.barrier)
        body = function_body(self.barrier, "Barrier_HighMountain")
        stages = (
            "barrier_platform_start_gesture()",
            "barrier_reverse_distance(6.0f, 12.0f, heading)",
            "barrier_play_platform_voice()",
            "vTaskDelay(\n        pdMS_TO_TICKS(\n            HIGH_MOUNTAIN_END_VOICE_WAIT_MS))",
            "high_mountain_finish_stop()",
        )
        positions = [body.index(stage) for stage in stages]
        self.assertEqual(positions, sorted(positions))
        for forbidden in (
            "Chassis_Turn_180_Blocking()",
            "high_mountain_descend(",
            "barrier_complete(",
        ):
            self.assertNotIn(forbidden, body)

    def test_finish_helper_disables_chassis_and_brakes_forever(self):
        body = function_body(self.barrier, "high_mountain_finish_stop")
        self.assertIn("Chassis_SetMode(is_No)", body)
        self.assertIn("CarBrake()", body)
        self.assertIn("while (1)", body)
        self.assertIn("vTaskDelay(pdMS_TO_TICKS(100u))", body)
```

Update the existing polar-mountain test that currently requires turn/descent/`barrier_complete()` so it instead checks the two ascent helpers, infrared detection, gesture, reverse, voice, and finish helper in order.

- [ ] **Step 2: Run test to verify it fails**

Run:

```text
python -m unittest tests.test_p8_finish_contract tests.test_polar_mountain_contract -v
```

Expected: the new route test fails because both arrays still contain `ROUTE_P8_RETURN_UPPER`, and the high-mountain test fails because the wait/helper are missing and the old return suffix remains.

### Task 2: Implement P8 route termination and permanent stop

**Files:**
- Modify: `App/map/route_catalog.c:59-70`
- Modify: `App/barrier/barrier.c:77-84, 196-220, 1602-1673`

**Interfaces:**
- Consumes: existing route macros, `barrier_platform_start_gesture()`, `barrier_reverse_distance()`, and `barrier_play_platform_voice()`.
- Produces: `high_mountain_finish_stop()` and the P8-terminal `Barrier_HighMountain()` success path.

- [ ] **Step 1: Remove only first-round return suffixes**

Change the arrays to:

```c
static const uint8_t round1_highscore[] = {
    ROUTE_TO_HIGH_SCORE,
    ROUTE_END
};

static const uint8_t round1_highscore_d2[] = {
    ROUTE_HIGH_FROM_N13,
    ROUTE_END
};
```

Do not edit `ROUTE_P8_RETURN_UPPER`, `BACK_*`, or `round2_*` declarations.

- [ ] **Step 2: Add the voice wait constant and finish helper**

Add beside the barrier timing constants:

```c
#define HIGH_MOUNTAIN_END_VOICE_WAIT_MS 3000u
```

Add beside the existing platform helpers:

```c
static void high_mountain_finish_stop(void)
{
    Chassis_SetMode(is_No);
    CarBrake();

    while (1)
    {
        CarBrake();
        vTaskDelay(pdMS_TO_TICKS(100u));
    }
}
```

- [ ] **Step 3: Replace only the high-mountain post-voice suffix**

Keep the existing gesture and reverse calls unchanged, then use:

```c
    CarBrake();
    barrier_play_platform_voice();
    vTaskDelay(
        pdMS_TO_TICKS(
            HIGH_MOUNTAIN_END_VOICE_WAIT_MS));
    high_mountain_finish_stop();
```

Remove the now-unreachable local `turn_target` and the success-path calls to `mpuZreset()`, `Chassis_Turn_180_Blocking()`, `barrier_platform_center()`, `high_mountain_descend()`, `Chassis_EnableLineLostProtection()`, and `barrier_complete()`. Leave all preceding failure checks intact.

- [ ] **Step 4: Run the focused tests to verify they pass**

Run:

```text
python -m unittest tests.test_p8_finish_contract tests.test_polar_mountain_contract -v
```

Expected: all tests in both modules pass.

### Task 3: Regression verification and firmware build

**Files:**
- Verify: `App/map/route_catalog.c`
- Verify: `App/barrier/barrier.c`
- Verify: `tests/test_p8_finish_contract.py`

**Interfaces:**
- Consumes: Task 2 source changes.
- Produces: fresh test and build evidence; no commit or hardware side effect.

- [ ] **Step 1: Run all source-contract tests**

Run:

```text
python -m unittest discover -s tests -p "test_*.py" -v
```

Record any pre-existing unrelated failure separately; do not weaken the new P8 contracts.

- [ ] **Step 2: Check the diff**

Run:

```text
git -c safe.directory="D:/stm32project/游中国探险/26--" -C "D:/stm32project/游中国探险/26--" diff --check
git -c safe.directory="D:/stm32project/游中国探险/26--" -C "D:/stm32project/游中国探险/26--" status --short
```

Confirm only the intended route/barrier/test/design/plan files are changed, with no generated firmware or unrelated source edits.

- [ ] **Step 3: Build the Release firmware**

Run:

```text
cmake --preset Release
cmake --build --preset Release --parallel 4
```

Expected: exit code 0 and a refreshed `build/Release/explorer_26.elf`. Report existing linker warnings separately if they remain.

- [ ] **Step 4: Re-run focused contracts after build**

Run:

```text
python -m unittest tests.test_p8_finish_contract tests.test_polar_mountain_contract -v
```

Use the fresh output as the completion evidence. Do not burn the artifact or claim physical validation.
